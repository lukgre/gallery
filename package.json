{
  "name": "gallery",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server app/",
    "deploy": "gh-pages -d app/"
  },
  "devDependencies": {
    "http-server": "^0.12.0",
    "gh-pages": "^2.1.1"
  }
}